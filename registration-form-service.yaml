apiVersion: v1
kind: Service
metadata:
  name: registration-service
  labels:
    app: registration
spec:
  type: NodePort
  selector:
    app: registration
  ports:
    - port: 80
      targetPort: 80
      nodePort: 31293
